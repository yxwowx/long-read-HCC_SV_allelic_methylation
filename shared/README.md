# shared

Common utilities sourced by nearly every script in this repo.

## Files
- `shared_utils.R` <- `SV-DMR/shared_file/pipeline/shared_utils.R` — **copied**
  (`reciprocal_overlap_hits()`, `filter_dmr_region()`, `anonym_sample()`, `PATIENT_MAP_PATH`)
